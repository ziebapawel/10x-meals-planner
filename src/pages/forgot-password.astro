---
import Layout from "../layouts/Layout.astro";
import { ForgotPasswordForm } from "../components/auth";

// TODO: Check if user is already authenticated and redirect to homepage
// This will be implemented when middleware and auth backend are ready
// if (Astro.locals.session) {
//   return Astro.redirect("/");
// }
---

<Layout title="Resetuj hasło - Planer Posiłków" showNav={false}>
  <ForgotPasswordForm client:load />
</Layout>

