---
import Layout from "../layouts/Layout.astro";
import { ResetPasswordForm } from "../components/auth";

// Note: Token validation is handled by Supabase automatically
// The reset token is in the URL fragment (#access_token=...) 
// and will be used by Supabase client when updateUser is called
---

<Layout title="Ustaw nowe hasło - Planer Posiłków" showNav={false}>
  <ResetPasswordForm client:load />
</Layout>
